import React from 'react'
import PropTypes from 'prop-types'

const UnderwritingCategories = props =>{
  return(
    <div>
    </div>
  )
}

UnderwritingCategories.propTypes = {
}

export default UnderwritingCategories